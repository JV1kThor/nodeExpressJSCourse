
//we can export from a module basically everything: variables, functions, lists, objects etc
const items = ["item1", "item2"]

const person = {
    name: "Bob",
}

module.exports.items = ["item1", "item2"]

module.exports.singlePerson = person